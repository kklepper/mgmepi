%% -*- erlang -*-
{application, mgmepi,
 [
  {description, "mgmepi : MySQL Cluster Management API (mgmapi) for Erlang"},
  {vsn, {cmd, "cat VERSION"}},
  {registered, [mgmepi_sup]},
  {included_applications, []},
  {applications, [kernel,stdlib]},
  {mod, {baseline_app, [
                        %% @see ../priv/conf/*.config
                       ]}}
 ]}.
